<div class="container">
  <div class="surveys">
    <h1><%=  User.all.first.username %></h1>
    <% User.all.first.surveys.each do |survey| %>
    <div class="name">
     <a href="/survey/<%=survey.id%>"><%= survey.name %></a> 
     <% end %>
   </div>
   <a href="/surveys/new">Create Survey</a>
 </div>
</div>

